apiVersion: apps/v1
kind: Deployment
metadata:
  name: twotierapp
  labels:
    app: twotierapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: twotierapp
  template:
    metadata:
      labels:
        app: twotierapp
    spec:
      containers:
        - name: twotierapp
          image: madhupdevops/two-tier-microservice-app:latest
          env:
            - name: MYSQL_HOST
              value: "10.98.19.211"          # this is your mysql's service clusture IP, Make sure to change it with yours
            - name: MYSQL_PASSWORD
              value: "admin"
            - name: MYSQL_USER
              value: "root"
            - name: MYSQL_DB
              value: "mydb"
          ports:
            - containerPort: 5000
          imagePullPolicy: Always
